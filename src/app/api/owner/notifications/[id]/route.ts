import { NextRequest, NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { requireRestaurantContext } from "@/lib/auth";
async function find(id: string) { const context = await requireRestaurantContext(); if (context.errorStatus) return { context, item: null }; const item = await prisma.notification.findFirst({ where: { id: Number(id), restaurantId: context.restaurantId!, recipientId: context.user!.id } }); return { context, item }; }
export async function GET(_request: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { context, item } = await find((await params).id); if (context.errorStatus) return NextResponse.json({ success: false, message: context.errorMessage }, { status: context.errorStatus }); if (!item) return NextResponse.json({ success: false, message: "Notification not found." }, { status: 404 }); return NextResponse.json({ success: true, data: item }); } catch (error) { console.error(error); return NextResponse.json({ success: false, message: "Failed to load notification." }, { status: 500 }); } }
export async function PATCH(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { context, item } = await find((await params).id); if (context.errorStatus) return NextResponse.json({ success: false, message: context.errorMessage }, { status: context.errorStatus }); if (!item) return NextResponse.json({ success: false, message: "Notification not found." }, { status: 404 }); const body = await request.json().catch(() => ({})); const isRead = typeof body.isRead === "boolean" ? body.isRead : !item.isRead; const updated = await prisma.notification.update({ where: { id: item.id }, data: { isRead, readAt: isRead ? new Date() : null } }); return NextResponse.json({ success: true, data: updated }); } catch (error) { console.error(error); return NextResponse.json({ success: false, message: "Could not update notification." }, { status: 500 }); } }
export async function DELETE(_request: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { context, item } = await find((await params).id); if (context.errorStatus) return NextResponse.json({ success: false, message: context.errorMessage }, { status: context.errorStatus }); if (!item) return NextResponse.json({ success: false, message: "Notification not found." }, { status: 404 }); await prisma.notification.delete({ where: { id: item.id } }); return NextResponse.json({ success: true, message: "Notification deleted." }); } catch (error) { console.error(error); return NextResponse.json({ success: false, message: "Could not delete notification." }, { status: 500 }); } }
